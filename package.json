{
  "name": "file-bridge",
  "version": "1.2.0",
  "private": true,
  "description": "A password-protected lightweight browser-based text and small-file transfer assistant using 4-digit pairing codes.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ipaddr.js": "^2.5.0",
    "proxy-addr": "^2.0.7",
    "socket.io": "^4.8.1",
    "svg-captcha": "^1.4.0"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
